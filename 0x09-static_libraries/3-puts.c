#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
putchar("Programming is like building a multilingual puzzle");
putchar('\n');
return (0);
}
