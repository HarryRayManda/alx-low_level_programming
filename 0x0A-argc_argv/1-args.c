#include "main.h"
#include <stdio.h>
#include <stdlib>

/**
 * main - Prints its name
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
(void) argv;
printf("argc = %d\n", argc);
return (0);
}
